# MCP Server Configuration
# Transport mode: stdio (for Claude Desktop) or sse (for Docker/HTTP)
MCP_TRANSPORT=sse

# HTTP server settings (only used when MCP_TRANSPORT=sse)
MCP_SERVER_HOST=0.0.0.0
MCP_SERVER_PORT=8000

# TLS/SSL settings (REQUIRED for Claude Desktop)
# Generate self-signed certs with: ./scripts/generate_certs.sh
MCP_SSL_CERTFILE=/app/certs/cert.pem
MCP_SSL_KEYFILE=/app/certs/key.pem

# DX Cluster Configuration
# The hostname or IP of the DX cluster server
DX_CLUSTER_HOST=dxc.nc7j.com

# The telnet port of the DX cluster (usually 7300 or 23)
DX_CLUSTER_PORT=7300

# Your amateur radio callsign or identifier
# Some clusters require a valid callsign for access
DX_CLUSTER_CALLSIGN=YOUR-CALLSIGN

# IARU Region (1, 2, or 3)
# Region 1: Europe, Africa, Middle East, Northern Asia
# Region 2: Americas (North and South America)
# Region 3: Asia-Pacific (rest of Asia, Oceania)
# This affects the amateur radio band frequency allocations
IARU_REGION=2

# Alternative DX Clusters:
# dxc.nc7j.com:7300 (NC7J DX Cluster)
# dxfun.com:8000 (DXFun)
# w6cua.no-ip.org:7300 (W6CUA)
# ve7cc.net:23 (VE7CC)
